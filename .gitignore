# Ignore these so they're not stored in source control
.vscode
/node_modules/
/docs/build.css
/tmp/
# NOTE: package-lock.json is intentionally tracked so GitHub's dependency graph
# and Dependabot can detect vulnerable transitive dependencies and open update PRs.
